<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/animations.css">
    <title>📦 Gestión de Productos - Admin PetShop</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .admin-container {
            flex: 1;
            padding: 2rem;
            max-width: 1400px;
            margin: 0 auto;
            width: 100%;
        }

        .page-header {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            margin-bottom: 2rem;
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .page-title {
            color: #2E8B57;
            font-size: 2rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .action-buttons {
            display: flex;
            gap: 1rem;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.8rem 1.5rem;
            border-radius: 10px;
            font-weight: bold;
            text-decoration: none;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .btn-primary {
            background: linear-gradient(135deg, #2E8B57 0%, #228B22 100%);
            color: white;
        }

        .btn-primary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(46, 139, 87, 0.4);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #6c757d 0%, #495057 100%);
            color: white;
        }

        .btn-secondary:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(108, 117, 125, 0.4);
        }

        .products-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
            gap: 2rem;
        }

        .product-card {
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .product-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }

        .product-image {
            width: 100%;
            height: 200px;
            object-fit: cover;
            background: #f8f9fa;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .product-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .product-info {
            padding: 1.5rem;
        }

        .product-name {
            font-size: 1.2rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 0.5rem;
        }

        .product-price {
            font-size: 1.1rem;
            color: #2E8B57;
            font-weight: bold;
            margin-bottom: 0.5rem;
        }

        .product-stock {
            font-size: 0.9rem;
            color: #666;
            margin-bottom: 1rem;
        }

        .stock-high {
            color: #28a745;
        }

        .stock-medium {
            color: #ffc107;
        }

        .stock-low {
            color: #dc3545;
        }

        .product-actions {
            display: flex;
            gap: 0.5rem;
            justify-content: center;
        }

        .btn-action {
            padding: 0.5rem 1rem;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.9rem;
        }

        .btn-edit {
            background: #17a2b8;
            color: white;
        }

        .btn-edit:hover {
            background: #138496;
        }

        .btn-delete {
            background: #dc3545;
            color: white;
        }

        .btn-delete:hover {
            background: #c82333;
        }

        .stats-bar {
            display: flex;
            justify-content: space-around;
            background: white;
            padding: 1.5rem;
            margin-bottom: 2rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-size: 2rem;
            font-weight: bold;
            color: #2E8B57;
        }

        .stat-label {
            color: #666;
            font-size: 0.9rem;
        }

        .empty-state {
            text-align: center;
            padding: 4rem 2rem;
            background: white;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .empty-icon {
            font-size: 4rem;
            color: #ddd;
            margin-bottom: 1rem;
        }

        @media (max-width: 768px) {
            .admin-container {
                padding: 1rem;
            }
            
            .page-header {
                flex-direction: column;
                text-align: center;
            }
            
            .products-grid {
                grid-template-columns: 1fr;
            }
            
            .stats-bar {
                flex-direction: column;
                gap: 1rem;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <main class="admin-container">
        <div class="page-header animate-fadeInUp">
            <h1 class="page-title">
                <i class="fas fa-box"></i>
                Gestión de Productos
            </h1>
            <div class="action-buttons">
                <a href="/admin/productos/crear" class="btn btn-primary">
                    <i class="fas fa-plus"></i>
                    Crear Producto
                </a>
                <a href="/admin/dashboard" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i>
                    Volver al Dashboard
                </a>
            </div>
        </div>

        <div class="stats-bar animate-fadeInUp delay-1">
            <div class="stat-item">
                <div class="stat-number"><%= productos.length %></div>
                <div class="stat-label">Total Productos</div>
            </div>
            <div class="stat-item">
                <div class="stat-number"><%= productos.filter(p => p.stock > 0).length %></div>
                <div class="stat-label">Con Stock</div>
            </div>
            <div class="stat-item">
                <div class="stat-number"><%= productos.filter(p => p.stock === 0).length %></div>
                <div class="stat-label">Sin Stock</div>
            </div>
            <div class="stat-item">
                <div class="stat-number">$<%= productos.reduce((sum, p) => sum + (p.precio * p.stock), 0).toFixed(2) %></div>
                <div class="stat-label">Valor Inventario</div>
            </div>
        </div>

        <% if (productos.length > 0) { %>
            <div class="products-grid">
                <% productos.forEach((producto, index) => { %>
                    <div class="product-card animate-fadeInUp delay-<%= (index % 6) + 1 %>">
                        <div class="product-image">
                            <% if (producto.img && producto.img !== 'default.jpg') { %>
                                <img src="/images/products/<%= producto.img %>" alt="<%= producto.nombre %>">
                            <% } else { %>
                                <i class="fas fa-image" style="font-size: 3rem; color: #ddd;"></i>
                            <% } %>
                        </div>
                        <div class="product-info">
                            <h3 class="product-name"><%= producto.nombre %></h3>
                            <p class="product-price">$<%= producto.precio %></p>
                            <p class="product-stock stock-<%= producto.stock > 10 ? 'high' : producto.stock > 5 ? 'medium' : 'low' %>">
                                Stock: <%= producto.stock %> unidades
                            </p>
                            <% if (producto.categoria) { %>
                                <p style="color: #666; font-size: 0.9rem; margin-bottom: 1rem;">
                                    <i class="fas fa-tag"></i> <%= producto.categoria %>
                                </p>
                            <% } %>
                            <div class="product-actions">
                                <a href="/admin/productos/editar/<%= producto.id %>" class="btn-action btn-edit">
                                    <i class="fas fa-edit"></i> Editar
                                </a>
                                <form action="/admin/productos/<%= producto.id %>?_method=DELETE" method="POST" style="display: inline;">
                                    <button type="submit" class="btn-action btn-delete" onclick="return confirm('¿Estás seguro de eliminar este producto?')">
                                        <i class="fas fa-trash"></i> Eliminar
                                    </button>
                                </form>
                            </div>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } else { %>
            <div class="empty-state animate-bounceIn">
                <i class="fas fa-box empty-icon"></i>
                <h3>No hay productos en el catálogo</h3>
                <p>Comienza agregando productos para que aparezcan aquí.</p>
                <a href="/admin/productos/crear" class="btn btn-primary" style="margin-top: 1rem;">
                    <i class="fas fa-plus"></i>
                    Crear Primer Producto
                </a>
            </div>
        <% } %>
    </main>

    <%- include('../partials/footer') %>
    
    <script src="/js/petshop-animations.js"></script>
</body>
</html>
