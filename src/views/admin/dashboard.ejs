<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet" />
    <link rel="stylesheet" href="/css/header.css">
    <link rel="stylesheet" href="/css/footer.css">
    <link rel="stylesheet" href="/css/animations.css">
    <title>🔧 Admin Dashboard - PetShop</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            margin: 0;
            padding: 0;
        }

        .admin-container {
            flex: 1;
            padding: 3rem 2rem 2rem 2rem;
            max-width: 1200px;
            margin: 0 auto;
            width: 100%;
        }

        .admin-header {
            background: linear-gradient(135deg, #2E8B57 0%, #228B22 100%);
            color: white;
            padding: 3rem 2rem;
            border-radius: 20px;
            box-shadow: 0 15px 40px rgba(0,0,0,0.2);
            margin-bottom: 3rem;
            text-align: center;
            position: relative;
            overflow: hidden;
        }

        .admin-header::before {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: url('/images/hueso-perro.jpg') repeat;
            opacity: 0.1;
            animation: float 20s infinite linear;
        }

        @keyframes float {
            0% { transform: translateX(-50px) translateY(-50px); }
            100% { transform: translateX(0px) translateY(0px); }
        }

        .admin-title {
            color: white;
            font-size: 3rem;
            margin-bottom: 1rem;
            position: relative;
            z-index: 1;
            text-shadow: 0 2px 4px rgba(0,0,0,0.3);
        }

        .admin-subtitle {
            color: rgba(255,255,255,0.9);
            font-size: 1.3rem;
            position: relative;
            z-index: 1;
            font-weight: 300;
        }

        .welcome-stats {
            display: flex;
            justify-content: center;
            gap: 2rem;
            margin-top: 2rem;
            position: relative;
            z-index: 1;
        }

        .welcome-stat {
            text-align: center;
            background: rgba(255,255,255,0.1);
            padding: 1rem 1.5rem;
            border-radius: 10px;
            backdrop-filter: blur(10px);
        }

        .welcome-stat-number {
            font-size: 1.8rem;
            font-weight: bold;
            display: block;
        }

        .welcome-stat-label {
            font-size: 0.9rem;
            opacity: 0.8;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
            margin-bottom: 3rem;
        }

        .stat-card {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            text-align: center;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 20px 40px rgba(0,0,0,0.15);
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: linear-gradient(135deg, #2E8B57 0%, #228B22 100%);
        }

        .stat-icon {
            font-size: 3rem;
            color: #2E8B57;
            margin-bottom: 1rem;
        }

        .stat-number {
            font-size: 2.5rem;
            font-weight: bold;
            color: #333;
            margin-bottom: 0.5rem;
        }

        .stat-label {
            color: #666;
            font-size: 1.1rem;
        }

        .actions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }

        .action-card {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
            transition: transform 0.3s ease;
        }

        .action-card:hover {
            transform: translateY(-5px);
        }

        .action-title {
            color: #2E8B57;
            font-size: 1.5rem;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .action-description {
            color: #666;
            margin-bottom: 1.5rem;
            line-height: 1.6;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.8rem 1.5rem;
            background: linear-gradient(135deg, #2E8B57 0%, #228B22 100%);
            color: white;
            text-decoration: none;
            border-radius: 10px;
            font-weight: bold;
            transition: all 0.3s ease;
            border: none;
            cursor: pointer;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 5px 15px rgba(46, 139, 87, 0.4);
        }

        .btn-secondary {
            background: linear-gradient(135deg, #6c757d 0%, #495057 100%);
        }

        .btn-danger {
            background: linear-gradient(135deg, #dc3545 0%, #c82333 100%);
        }

        .recent-section {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 2rem;
            margin-top: 2rem;
        }

        .recent-card {
            background: white;
            padding: 2rem;
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.1);
        }

        .recent-title {
            color: #2E8B57;
            font-size: 1.3rem;
            margin-bottom: 1rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .recent-list {
            list-style: none;
        }

        .recent-item {
            padding: 0.8rem 0;
            border-bottom: 1px solid #eee;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .recent-item:last-child {
            border-bottom: none;
        }

        .item-info {
            display: flex;
            flex-direction: column;
        }

        .item-name {
            font-weight: bold;
            color: #333;
        }

        .item-detail {
            font-size: 0.9rem;
            color: #666;
        }

        .quick-stats {
            display: flex;
            justify-content: space-around;
            background: #f8f9fa;
            padding: 1rem;
            border-radius: 10px;
            margin-top: 1rem;
        }

        .quick-stat {
            text-align: center;
        }

        .quick-stat-number {
            font-size: 1.5rem;
            font-weight: bold;
            color: #2E8B57;
        }

        .quick-stat-label {
            font-size: 0.9rem;
            color: #666;
        }

        @media (max-width: 768px) {
            .admin-container {
                padding: 1rem;
            }
            
            .admin-header {
                padding: 2rem 1rem;
                margin-bottom: 2rem;
            }
            
            .admin-title {
                font-size: 2rem;
            }
            
            .admin-subtitle {
                font-size: 1rem;
            }
            
            .welcome-stats {
                flex-direction: column;
                gap: 1rem;
            }
            
            .welcome-stat {
                padding: 0.8rem 1rem;
            }
            
            .stats-grid {
                grid-template-columns: 1fr;
            }
            
            .actions-grid {
                grid-template-columns: 1fr;
            }
            
            .recent-section {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <%- include('../partials/header') %>

    <main class="admin-container">
        <div class="admin-header animate-fadeInUp">
            <h1 class="admin-title">
                <i class="fas fa-tachometer-alt"></i>
                Panel de Administración PetShop
            </h1>
            <p class="admin-subtitle">Bienvenido de vuelta, <%= user.firstName %> <%= user.lastName %></p>
            <div class="welcome-stats">
                <div class="welcome-stat">
                    <span class="welcome-stat-number"><%= stats.totalUsuarios %></span>
                    <span class="welcome-stat-label">Usuarios Totales</span>
                </div>
                <div class="welcome-stat">
                    <span class="welcome-stat-number"><%= stats.totalProductos %></span>
                    <span class="welcome-stat-label">Productos en Catálogo</span>
                </div>
                <div class="welcome-stat">
                    <span class="welcome-stat-number"><%= stats.productosActivos %></span>
                    <span class="welcome-stat-label">Con Stock Disponible</span>
                </div>
            </div>
        </div>

        <div class="stats-grid">
            <div class="stat-card animate-fadeInUp delay-1">
                <i class="fas fa-users stat-icon"></i>
                <div class="stat-number"><%= stats.totalUsuarios %></div>
                <div class="stat-label">Total Usuarios</div>
            </div>
            <div class="stat-card animate-fadeInUp delay-2">
                <i class="fas fa-box stat-icon"></i>
                <div class="stat-number"><%= stats.totalProductos %></div>
                <div class="stat-label">Total Productos</div>
            </div>
            <div class="stat-card animate-fadeInUp delay-3">
                <i class="fas fa-user-check stat-icon"></i>
                <div class="stat-number"><%= stats.clientesRegulares %></div>
                <div class="stat-label">Clientes</div>
            </div>
            <div class="stat-card animate-fadeInUp delay-4">
                <i class="fas fa-warehouse stat-icon"></i>
                <div class="stat-number"><%= stats.productosActivos %></div>
                <div class="stat-label">Con Stock</div>
            </div>
        </div>

        <div class="actions-grid">
            <div class="action-card animate-fadeInLeft delay-1">
                <h3 class="action-title">
                    <i class="fas fa-users"></i>
                    Gestionar Usuarios
                </h3>
                <p class="action-description">
                    Administra los usuarios registrados, cambia roles, elimina cuentas y supervisa la actividad.
                </p>
                <a href="/admin/usuarios" class="btn">
                    <i class="fas fa-cog"></i>
                    Gestionar Usuarios
                </a>
            </div>

            <div class="action-card animate-fadeInLeft delay-2">
                <h3 class="action-title">
                    <i class="fas fa-box"></i>
                    Gestionar Productos
                </h3>
                <p class="action-description">
                    Añade, edita o elimina productos del catálogo. Controla el inventario y precios.
                </p>
                <a href="/admin/productos" class="btn">
                    <i class="fas fa-edit"></i>
                    Gestionar Productos
                </a>
            </div>

            <div class="action-card animate-fadeInLeft delay-3">
                <h3 class="action-title">
                    <i class="fas fa-plus-circle"></i>
                    Crear Producto
                </h3>
                <p class="action-description">
                    Agrega nuevos productos al catálogo con imágenes, descripciones y precios.
                </p>
                <a href="/admin/productos/crear" class="btn">
                    <i class="fas fa-plus"></i>
                    Crear Producto
                </a>
            </div>

            <div class="action-card animate-fadeInLeft delay-4">
                <h3 class="action-title">
                    <i class="fas fa-chart-bar"></i>
                    Ver Estadísticas
                </h3>
                <p class="action-description">
                    Accede a estadísticas detalladas de ventas, usuarios y productos más populares.
                </p>
                <a href="/admin/estadisticas" class="btn">
                    <i class="fas fa-chart-line"></i>
                    Ver Estadísticas
                </a>
            </div>
        </div>

        <div class="recent-section">
            <div class="recent-card animate-fadeInUp delay-5">
                <h3 class="recent-title">
                    <i class="fas fa-user-plus"></i>
                    Usuarios Recientes
                </h3>
                <ul class="recent-list">
                    <% usuarios.forEach(usuario => { %>
                        <li class="recent-item">
                            <div class="item-info">
                                <span class="item-name"><%= usuario.firstName %> <%= usuario.lastName %></span>
                                <span class="item-detail"><%= usuario.email %> - <%= usuario.category %></span>
                            </div>
                        </li>
                    <% }); %>
                </ul>
                <div class="quick-stats">
                    <div class="quick-stat">
                        <div class="quick-stat-number"><%= stats.clientesRegulares %></div>
                        <div class="quick-stat-label">Clientes</div>
                    </div>
                    <div class="quick-stat">
                        <div class="quick-stat-number"><%= stats.administradores %></div>
                        <div class="quick-stat-label">Admins</div>
                    </div>
                </div>
            </div>

            <div class="recent-card animate-fadeInUp delay-6">
                <h3 class="recent-title">
                    <i class="fas fa-box"></i>
                    Productos Recientes
                </h3>
                <ul class="recent-list">
                    <% productos.forEach(producto => { %>
                        <li class="recent-item">
                            <div class="item-info">
                                <span class="item-name"><%= producto.name %></span>
                                <span class="item-detail">$<%= producto.price %> - Stock: <%= producto.stock || 0 %></span>
                            </div>
                        </li>
                    <% }); %>
                </ul>
                <div class="quick-stats">
                    <div class="quick-stat">
                        <div class="quick-stat-number"><%= stats.productosActivos %></div>
                        <div class="quick-stat-label">Con Stock</div>
                    </div>
                    <div class="quick-stat">
                        <div class="quick-stat-number"><%= stats.productosSinStock %></div>
                        <div class="quick-stat-label">Sin Stock</div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <%- include('../partials/footer') %>
    
    <script src="/js/petshop-animations.js"></script>
</body>
</html>
